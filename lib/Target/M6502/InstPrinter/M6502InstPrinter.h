// TODO: header stuff

#ifndef LLVM_LIB_TARGET_M6502_INSTPRINTER_M6502INSTPRINTER_H
#define LLVM_LIB_TARGET_M6502_INSTPRINTER_M6502INSTPRINTER_H

#include "llvm/MC/MCInstPrinter.h"

namespace llvm {

class M6502InstPrinter : public MCInstPrinter {
public:
  M6502InstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
                   const MCRegisterInfo &MRI)
      : MCInstPrinter(MAI, MII, MRI) {}

  void printInst(const MCInst *MI, raw_ostream &O, StringRef Annot,
                 const MCSubtargetInfo &STI) override;

  // Used by tblgenned code.
  void printOperand(const MCInst *MI, unsigned OpNo, raw_ostream &O);

  // Autogenerated by tblgen.
  void printInstruction(const MCInst *MI, raw_ostream &O);
  static const char *getRegisterName(unsigned RegNo);

private:
};

} // end namespace llvm

#endif
